# ── PugaX Trade — docker-compose.yml ─────────────────────────────────────────
services:
  pugaxtrade:
    build:
      context: .
      dockerfile: Dockerfile
    image: pugaxtrade:latest
    container_name: pugaxtrade_app
    restart: unless-stopped

    ports:
      - "3838:3838"

    networks:
      - dokploy-network

    environment:
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL}

      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}
      STRIPE_PUBLISHABLE_KEY: ${STRIPE_PUBLISHABLE_KEY}
      STRIPE_PRICE_ID: ${STRIPE_PRICE_ID}
      STRIPE_ALLOW_TEST: ${STRIPE_ALLOW_TEST}

      PGHOST: ${PGHOST}
      PGPORT: ${PGPORT}
      PGDATABASE: ${PGDATABASE}
      PGUSER: ${PGUSER}
      PGPASSWORD: ${PGPASSWORD}
      PGSSLMODE: ${PGSSLMODE}

      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASS: ${SMTP_PASS}
      SMTP_FROM: ${SMTP_FROM}
      SMTP_ADMIN_EMAIL: ${SMTP_ADMIN_EMAIL}

    volumes:
      - shiny_logs:/var/log/shiny-server

    healthcheck:
      test: ["CMD-SHELL", "curl -fsS -H 'Host: localhost' http://127.0.0.1:3838/ >/dev/null || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 90s

volumes:
  shiny_logs:

networks:
  dokploy-network:
    external: true
