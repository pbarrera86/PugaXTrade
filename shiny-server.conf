run_as shiny;

server {
  listen 3838;

  location / {
    site_dir /srv/shiny-server;
    log_dir /var/log/shiny-server;
    app_idle_timeout 0;
    reconnect true;
  }

  location /pugaxtrade {
    site_dir /srv/shiny-server;
    log_dir /var/log/shiny-server;
    app_idle_timeout 0;
    reconnect true;
  }
}
